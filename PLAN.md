# 🚨 총각 검거 작전 — 결혼 축하 파티 게임 사이트 기획서

> 컨셉: **1번(긴급속보/검거) + 4번(교정본부/수감)** 통합 세계관
> 구조: **현섭 1 vs 친구들 다수** — 현섭이 이겨야 보석금(축하금) 적립
> 메인 화면: **펜션 TV (노트북 연결, 가로 대화면)** / 서브: 각자 핸드폰
>
> 🗓 **총각파티: 2026-08-08 ~ 08-09** / **결혼식: 2026-11-10** (파티 시점 기준 D-94)
> ⏰ **개발 가용 기간: 8/3 ~ 8/7 (5일)**

---

## 1. 세계관 / 스토리라인

```
[속보] 신랑 현섭 검거    →    교정본부 수감 (무기징역)
        ↓
   교화 프로그램(게임) 수행  ← 친구들이 방해
        ↓
   클리어할 때마다 보석금 적립 + 형량 감형
        ↓
   실패 시 배우자 진술 대조 심사(천생연분)로 부활 기회
        ↓
   최종 출소 = 결혼식, 적립된 보석금 = 결혼 선물
```

**용어 치환표** (사이트 전체에서 이 단어만 사용)

| 실제 | 사이트 표기 |
|---|---|
| 상금 / 축하금 | **보석금(保釋金)** |
| 게임 | **교화 프로그램 / 집행** |
| 성공 | **감형** |
| 실패 | **징계 처분** |
| 신랑 현섭 | **수감번호 2026-LOVE-001** |
| 친구들 | **용의자 / 교도관** |
| 신부 명빈 | **배우자 진술인** |
| 최종 결과 | **출소 증명서** |

**게임별 이름**

| 실제 게임 | 사이트 표기 |
|---|---|
| 범인찾기 | **1차 집행 · 용의자 대질신문** |
| 이구이성 | **2차 집행 · 다중 음성 감청** |
| 천생연분 (보너스) | **긴급 · 배우자 진술 대조 심사** |

---

## 2. 화면 구조 (2-스크린 방식)

여기가 이번 설계의 핵심.

```
┌─────────────────────────┐        ┌──────────────────┐
│   📺 TV (노트북 연결)      │  ←동기화→ │ 📱 진행자(사회자) 폰 │
│   = 관전 화면              │        │  = 컨트롤러       │
│   모두가 보는 화면          │        │  정답/조작 버튼    │
│   정답은 절대 안 보임       │        │  혼자만 봄         │
└─────────────────────────┘        └──────────────────┘
              ↑ 같은 화면(읽기전용)
        📱📱 나머지 친구들 폰 (관전)
```

- **TV 화면**: 크게 보이는 연출 전용. 점수, 타이머, 보석금 게이지, 연출 애니메이션
- **진행자 폰**: 정답 단어 확인, 다음 문제, 정답/오답 판정, 청취 횟수 차감, 상금 지급
- **친구들 폰**: TV와 같은 내용 (자기 자리에서도 보이게) + 응원/야유 버튼(선택)

> ⚠️ 이구이성은 정답 단어가 TV에 뜨면 게임이 성립 안 됨 → 진행자 폰 분리가 필수

**TV 화면 설계 기준**: 1920×1080 가로, **3~4m 떨어져서 봐도 읽히는 크기**
→ 최소 폰트 32px, 핵심 숫자는 120px 이상

---

## 3. 페이지 목록

| # | 화면 | 내용 |
|---|---|---|
| 1 | **긴급속보 인트로** | 사이렌 + 경광등, "드디어 한 명 잡혀갑니다", D-DAY 카운트다운 |
| 2 | **교정본부 대시보드** | 보석금 현황(메인), 감형률 게이지, 집행 목록 |
| 3 | **수감자 프로필** | 머그샷(신장눈금 배경), 죄명/형량/특이사항 |
| 4 | **1차 집행: 용의자 대질신문** | 범인찾기 진행 화면 |
| 5 | **2차 집행: 다중 음성 감청** | 이구이성 진행 화면 |
| 6 | **긴급 심사: 천생연분** | 보너스 라운드 |
| 7 | **출소 증명서** | 최종 금액 + 이미지 저장 |
| C | **진행자 컨트롤러** | 폰 전용, 위 전부 조작 |

---

## 4. 게임별 화면 상세 설계

### 🔴 1차 집행 — 용의자 대질신문 (범인찾기)

**룰**: 3라운드 진행, 그중 **1번이라도 벌칙 음식 먹은 사람을 찾아내면 성공**
준비물: 콜라/간장, 홈런볼/와사비홈런볼, 사이다/식초

**TV 화면**
```
┌────────────────────────────────────────┐
│  1차 집행 · 용의자 대질신문     ROUND 2/3  │
│  ─────────────────────────────────────  │
│  이번 라운드 증거물:  🥤 콜라 vs 간장       │
│                                        │
│  [ 용의자 라인업 — 머그샷 카드 나열 ]        │
│   👤    👤    👤    👤    👤              │
│   01    02    03    04    05             │
│                                        │
│  ROUND 1 ❌   ROUND 2 ⏳   ROUND 3 ⬜     │
│  성공 시 적립: 100,000원                  │
└────────────────────────────────────────┘
```
- 용의자 카드 = **머그샷 스타일**(신장눈금 배경 + 수감번호). 4번 컨셉 그대로 재활용
- 현섭이 지목하면 → 해당 카드에 **빨간 조준선 + 사이렌 점멸**
- 진행자가 판정 → 정답이면 카드에 **"유죄" 도장 쾅** + 지폐 흩날림 + 캐시 사운드
- 오답이면 진범 카드가 뒤늦게 뒤집히며 **"무죄 석방"** + 철문 닫히는 소리
- 3라운드 표시는 항상 하단에 (○/❌/대기)

**진행자 폰**: 라운드별 벌칙 음식 배정자 기록 → 지목 대상 선택 → [적중]/[빗나감] 버튼

---

### 🎧 2차 집행 — 다중 음성 감청 (이구이성)

**룰**: 5문제 중 3문제 이상 정답 시 성공. 문제당 최대 3회 청취
준비물: 3~5글자 단어 리스트

**TV 화면**
```
┌────────────────────────────────────────┐
│  2차 집행 · 다중 음성 감청      Q3 / 5     │
│  ─────────────────────────────────────  │
│                                        │
│        ⬛  ⬛  ⬛  ⬛  ⬛                  │
│         5 글자                          │
│                                        │
│     🔊 청취 잔여   ●  ●  ○              │
│                                        │
│  Q1 ⭕  Q2 ❌  Q3 ⏳  Q4 ⬜  Q5 ⬜        │
│  현재 2/3 · 목표까지 1문제                 │
└────────────────────────────────────────┘
```
- **글자수만큼 검은 블록**만 표시 (정답은 절대 노출 X)
- 진행자가 "발성" 버튼 누르면 → TV에 **3-2-1 카운트다운 + 신호음** (친구들이 동시에 외치는 타이밍 맞춤용)
- 청취 잔여 3→2→1, 0 되면 블록이 빨갛게 점멸
- 정답 시 → 글자 블록이 **하나씩 순서대로 뒤집히며 공개** + 적립 연출
- 오답 시 → 블록 전체 빨강 + "감청 실패" 도장

**진행자 폰**: 정답 단어 + 다음 문제 미리보기, [발성 신호] [정답] [오답] [청취 차감] 버튼

---

### 💗 긴급 심사 — 천생연분 (보너스)

**룰**: 본 게임 실패 시 발동. 신부(명빈) 인터뷰 답변을 맞히면 **해당 라운드 1회 부활**
못 맞히면 벌칙 음식 먹고 기회 획득(옵션)

**TV 화면**
- 앞의 살벌한 톤과 **의도적으로 반전** — 분홍/하트/반짝이 (촌스러움 극대화)
- 신부 사진 + 질문 카드가 화면 중앙에 크게
- 현섭이 답하면 진행자가 공개 → **카드 뒤집기 애니메이션**으로 실제 답변 등장
- 정답: 하트 폭발 + "천생연분 인증" 도장
- 오답: 카드 찢어지는 연출 + "벌칙 집행" → 벌칙 음식 화면

**진행자 폰**: 사전 인터뷰 질문/답변 목록, [정답][오답] 버튼

---

## 5. 보석금 적립 설계

게임이 2개뿐이라 **통짜로 주면 게이지가 안 움직여서 밋밋함**
→ **라운드/문제 단위로 잘게 쪼개서 적립**하는 걸 추천 (숫자가 계속 오르는 맛)

```
총 보석금 풀: (친구들이 모은 금액)

1차 집행 · 범인찾기
  ├ 라운드별 적중          → 라운드당 적립
  └ 3라운드 중 1회 이상 적중 → 클리어 보너스

2차 집행 · 음성 감청
  ├ 문제별 정답            → 문제당 적립
  ├ 3문제 이상             → 클리어 보너스
  └ 1회 청취만에 정답        → 추가 보너스

천생연분 부활 성공 시      → 해당 라운드 금액의 일부만 적립(예: 70%)

미달성 잔액 → 신랑 술값 / 벌칙으로 전환
```

- 화면 상단에 **보석금 바 고정 노출** (항상 보임)
- 적립 순간: 숫자 롤링 카운터 + 지폐/코인 쏟아짐 + 캐시 사운드 + 감형 게이지 상승
- 적립 이력 타임라인: `21:34 · 2차 집행 Q3 정답 → +50,000원`

---

## 6. 데이터 구조 (초안)

```ts
Groom     { name: '현섭', photo, weddingDate, crimes[], sentence }
Bride     { name: '명빈', photo }
Suspect   { id, name, photo, prisonNo }          // 친구들
Game      { id, title, type: 'culprit'|'voice'|'bonus',
            rounds, status, prizePerRound, clearBonus }
Round     { gameId, index, result: 'pending'|'win'|'lose',
            answer?, culpritId?, listensUsed? }
Question  { gameId, word, revealed }              // 이구이성 단어
Interview { q, a }                                // 천생연분 문답
PrizeLog  { at, gameId, roundIndex, amount, type }
State     { totalPool, earned, currentScreen, ... }  // 실시간 동기화 대상
```

### 설정 파일 (`config.json`) — 미정 항목 전부 여기로

보석금·인원 등 아직 안 정해진 값은 **코드에 박지 않고 설정 파일 하나로 분리**.
서버의 `/opt/marry/config.json`을 수정하고 재시작하면 반영 (재배포 불필요).

```jsonc
{
  "party":   { "date": "2026-08-08", "weddingDate": "2026-11-10" },
  "groom":   { "name": "현섭", "photo": "/img/groom.jpg", "prisonNo": "2026-LOVE-001" },
  "bride":   { "name": "명빈", "photo": "/img/bride.jpg" },

  "prize":   { "totalPool": 0, "currency": "원" },      // ← 금액 정해지면 입력

  "suspects": [                                          // ← 인원 정해지면 추가
    { "id": 1, "name": "", "photo": "" }
  ],

  "games": [                                             // ← 게임 추가는 여기에
    { "id": "culprit", "type": "culprit", "title": "1차 집행 · 용의자 대질신문",
      "rounds": 3, "prizePerRound": 0, "clearBonus": 0 },
    { "id": "voice",   "type": "voice",   "title": "2차 집행 · 다중 음성 감청",
      "questions": ["아메리카노"], "maxListens": 3, "clearThreshold": 3,
      "prizePerRound": 0, "clearBonus": 0 },
    { "id": "bonus",   "type": "bonus",   "title": "긴급 · 배우자 진술 대조 심사",
      "interviews": [{ "q": "", "a": "" }] }
  ],

  "controlPin": "0000"
}
```

- **인원 수는 하드코딩 금지** — 용의자 라인업은 `suspects` 배열 길이에 맞춰 자동 배치 (4명이든 12명이든 레이아웃 자동 대응)
- **보석금 배분** — `totalPool`만 넣으면 라운드 수에 따라 자동 분배하는 모드 + 라운드별 수동 지정 모드 둘 다 지원

### 🔧 게임 확장 구조 (게임 더 추가 예정이므로)

게임을 나중에 쉽게 붙일 수 있게 **타입별 렌더러 등록 방식**으로 설계.

```
config.games[]  →  type 값으로 렌더러 매칭
                    ├ 'culprit'  → 범인찾기 화면
                    ├ 'voice'    → 이구이성 화면
                    ├ 'bonus'    → 천생연분 화면
                    └ 'quiz' 등  → 새 게임 추가 시 여기에 등록
```

새 게임 = **TV 화면 컴포넌트 1개 + 컨트롤러 컴포넌트 1개 + config 항목 1개**.
대시보드·보석금·타임라인·연출은 공통 모듈이라 새로 안 만들어도 됨.

> 범용으로 미리 만들어두면 좋은 타입: `score`(단순 점수 누적형), `timer`(제한시간 도전형), `ox`(O/X 판정형) — 어떤 게임이 추가돼도 대충 커버됨

---

## 7. 기술 스택 & 배포

### 배포 대상 서버 (기존 Vultr 서버 재사용)

| 항목 | 값 |
|---|---|
| 접속 주소 | `https://158-247-222-58.sslip.io` (Caddy 자동 HTTPS) |
| 서버 | Vultr Seoul, `158.247.222.58` |
| SSH | `ssh -i ~/.ssh/ai_trader_vultr root@158.247.222.58` (키 전용) |
| 앱 포트 | `127.0.0.1:8000` → Caddy `reverse_proxy` |
| 보안 | ufw(22/80/443), fail2ban, Caddy basic auth |

> 서버 계정/비밀번호는 이 문서에 기록하지 않음. 별도 보관.

### 구성

```
https://158-247-222-58.sslip.io
 ├─ /          관전 화면   ← 노트북 전체화면 → HDMI로 TV 출력
 │                        ← 친구들 폰도 이 주소로 접속 (QR 스캔)
 └─ /control   컨트롤러    ← 진행자 폰 전용 (PIN 잠금)

              ↕ WebSocket (자체 서버)
    한 명이 조작하면 TV + 모든 폰이 동시에 반응
```

| 항목 | 선택 | 비고 |
|---|---|---|
| 프론트 | React + Vite + TypeScript | |
| 스타일 | Tailwind CSS | |
| 애니메이션 | Framer Motion | 도장/지폐/롤링/카드뒤집기 |
| 사운드 | Howler.js | 사이렌·캐시·철문·알람 |
| 서버 | **Node + Fastify + ws** | 정적 파일 + WebSocket 한 프로세스 |
| 상태 저장 | **서버 메모리 + JSON 파일** | DB 불필요. 재시작해도 상태 복원 |
| 프로세스 관리 | **systemd** (`marry.service`) | 서버 재부팅해도 자동 기동 |
| 배포 | 빌드 → `scp` → 서버 재시작 (`deploy.ps1` 스크립트화) | |

**Supabase 불필요** — 자체 서버가 있으므로 WebSocket을 직접 운용. 외부 서비스 의존이 없어 더 단순하고 지연도 적음. Caddy v2는 `reverse_proxy`가 WebSocket을 그대로 통과시킴(별도 설정 없음).

**Caddyfile 수정** (딱 한 줄)
```
respond "새 프로젝트 준비 중"   →   reverse_proxy 127.0.0.1:8000
```

### 접속 편의
- 인트로/대기 화면에 **QR 코드 상시 노출** → 친구들은 스캔만 하면 접속
- ⚠️ **Caddy basic auth 처리 결정 필요**: 현재 전 경로에 걸려 있어서, 친구들이 폰으로 접속할 때마다 아이디/비번 팝업이 뜸(QR 스캔해도 마찬가지). 10명 넘게 이걸 입력하면 흐름이 끊김.
  → **권장: 파티 사이트 경로는 basic auth 해제**하고, 민감한 `/control`만 앱 자체 PIN으로 보호. sslip.io 주소는 사실상 추측 불가능하고 노출돼도 피해가 없음.

### 🔒 컨트롤러 보안 (중요)
`/control`은 **반드시 PIN 잠금**. 안 걸면 친구가 컨트롤러 페이지 열어서 이구이성 정답을 미리 봄.
- 4자리 PIN, 진행자만 공유
- 컨트롤러 접속자 수를 TV에 표시해 몰래 들어온 사람 감지(선택)

### 🛠 현장(펜션)에서 수정하기 — 3단 방어

파티 당일에 뭔가 고쳐야 할 상황은 반드시 생김. **코드를 고칠 일 자체를 없애는 게 1순위**, 그다음이 배포 속도.

#### 1단 — 어드민 페이지 (배포 불필요, 즉시 반영) ⭐ 여기서 90% 해결
브라우저에서 `config`를 직접 수정. 진행자 폰으로도 가능.
- 보석금 총액·라운드별 금액 수정
- 참가자 추가/삭제/이름 변경 (당일에 누가 못 오거나 늦게 합류해도 대응)
- 이구이성 단어 추가/수정, 천생연분 질문 추가
- 게임 순서 변경 / 특정 게임 건너뛰기

#### 1단-B — 컨트롤러 "즉흥 대응" 버튼 (가장 중요)
현장에선 룰이 즉흥적으로 바뀜. *"이번엔 봐주자"*, *"보너스 주자"*, *"방금 건 무효"*.
이걸 버튼으로 미리 넣어두면 **코드 고칠 일이 거의 사라짐.**
- 💰 **보석금 수동 가감** (+/- 임의 금액, 사유 입력)
- ↩️ **직전 판정 되돌리기** (오조작 복구)
- ⏭️ **라운드/게임 건너뛰기**, **라운드 추가**
- 🔄 **청취 횟수 임의 조정**, **타이머 일시정지/연장**
- 🎬 **화면 강제 전환** (아무 화면으로나 점프)

> 이 버튼들이 없으면 사소한 상황마다 배포를 다시 해야 함. **필수 구현.**

#### 2단 — 원커맨드 배포 (코드를 진짜 고쳐야 할 때)
```powershell
npm run deploy
```
→ 로컬 빌드 → `scp`로 서버 전송 → `systemctl restart marry` → 완료. **약 20~30초.**
(옵션: 서버에 bare git repo + `post-receive` 훅을 두면 `git push server main` 한 방으로도 가능 — Vercel과 동일한 사용감)

#### 3단 — 로컬 모드 (최후의 보루, 그리고 실은 가장 빠름)
```powershell
npm run dev
```
노트북에서 실행하면 **HMR로 저장 즉시 화면 반영. 배포 과정 자체가 없음.**
폰들은 `http://192.168.x.x:5173`으로 접속. 인터넷 불필요.
→ 현장에서 코드를 계속 만져야 하는 상황이면 **아예 처음부터 로컬 모드로 진행하는 게 제일 편함.**

#### ✅ 사전 준비물 (이것만 지키면 됨)
- [ ] 노트북에 Node + 프로젝트 레포 클론 + `npm install` 완료 상태로 가져가기
- [ ] SSH 키(`ai_trader_vultr`)를 노트북에도 복사
- [ ] `npm run deploy` 를 파티 전에 최소 1회 성공시켜 보기
- [ ] `npm run dev` 로컬 모드도 폰 접속까지 1회 테스트

> ⚠️ Vercel/Netlify를 써도 위 준비물은 똑같이 필요함. 노트북에 레포 없으면 어느 방식이든 수정 불가.

### ⚠️ 인터넷 끊김 대비 (필수 폴백)
서버가 원격이므로 펜션 인터넷이 죽으면 게임이 중단됨. 대비책:
1. **로컬 백업 모드** — 앱이 자체 완결형 Node 서버라 **노트북에서 `npm start` 하면 완전히 동일하게 동작**. 인터넷이 죽으면 노트북을 서버로 전환하고 폰들은 같은 와이파이로 접속. (자체 서버 방식의 가장 큰 장점)
2. **연결 상태 표시등** — 끊기면 화면 상단에 경고, 조작은 로컬 캐시로 계속되고 복구 시 자동 재동기화
3. **폰 LTE 핫스팟** — 최악의 경우 노트북+진행자폰만 핫스팟에 연결해도 진행 가능
4. **사전 리허설** — 8/7에 실제 기기로 한 번 돌려보기

---

## 8. 디자인 가이드

**컬러**
- 경광등 레드 `#E10600` / 블루 `#0047FF`
- 폴리스 옐로우 `#FFD400`
- 콘크리트 그레이 `#3A3A3A` ~ `#1A1A1A`
- 금속 실버 그라데이션 (증명서·명패)
- 천생연분 전용: 핫핑크 `#FF3E9D` + 금색 반짝이

**폰트** (Google Fonts, 무료)
- 제목: `Black Han Sans`
- 본문: `Noto Sans KR Bold`
- 숫자/증서: `Bebas Neue` + 명조 혼용

**필수 연출**
- 폴리스라인 테이프 (기울어져 화면 가로지름)
- 경광등 빨강↔파랑 번갈아 번쩍
- 스캔라인/노이즈 텍스처 오버레이
- 도장 "쾅" (스케일다운 + 화면 흔들림)
- 쇠사슬 프레임, 철창 그림자
- 절제 금지. 과한 게 정답.

---

## 9. 개발 일정 (8/3 ~ 8/7, 5일)

파티가 **8/8**이라 시간이 빠듯함. 우선순위를 명확히 나눠서 진행.

| 일자 | 작업 | 완료 기준 |
|---|---|---|
| **8/3 (D-5)** | 프로젝트 셋업 · 디자인 시스템 · **서버 배포 파이프라인 + WebSocket 동기화 뼈대** | 서버 URL 접속 → 컨트롤러 버튼 누르면 TV 화면 반응 |
| **8/4 (D-4)** | 긴급속보 인트로 · 교정본부 대시보드 · 보석금 카운터/적립 연출 | 상금 올라가는 연출 완성 |
| **8/5 (D-3)** | **1차 집행(범인찾기)** · 수감자 프로필/머그샷 | 게임1 처음부터 끝까지 진행 가능 |
| **8/6 (D-2)** | **2차 집행(이구이성)** · **천생연분 보너스** | 게임2+보너스 진행 가능 |
| **8/7 (D-1)** | 추가 게임 · 출소 증명서 · 효과음 · TV 가독성 튜닝 · **리허설** | 실제 노트북+TV+폰으로 전체 리허설 1회 |

**1일차에 배포를 먼저 뚫는 게 핵심.** 서버·Caddy·WebSocket은 막히면 하루 날아갈 수 있는 구간이라, 화면 만들기 전에 "빈 화면이라도 URL로 접속되고 동기화되는 상태"를 먼저 확보한다.

**우선순위 (시간 부족 시 이 순서로 잘라냄)**
- 🔴 필수: 동기화 · 대시보드/보석금 · 게임1 · 게임2 · 컨트롤러 · **즉흥 대응 버튼** · **`npm run deploy` 스크립트**
- 🟡 중요: 인트로 연출 · 천생연분 · 머그샷 프로필 · **어드민(config 편집) 페이지**
- 🟢 여유되면: 출소 증명서 이미지 저장 · 응원/야유 버튼 · 명예의 전당

> 배포 스크립트와 즉흥 대응 버튼은 "나중에 붙이는 편의기능"이 아니라 **1일차·컨트롤러 만들 때 같이 넣는 필수 항목**. 현장 대응력이 여기서 갈림.

---

## 10. 아직 필요한 정보

**나중에 config.json에 넣으면 되는 것** (개발 진행에는 지장 없음)
- 총 보석금 금액
- 친구 인원수 / 이름
- 사진 — 현섭, 친구들(머그샷용), 명빈 님
- 이구이성 단어 리스트, 천생연분 인터뷰 질문/답변, 현섭 죄목 리스트

**빨리 정해줘야 하는 것**
1. **추가 게임 내용** — 8/7에 붙이려면 늦어도 8/6까지는 룰이 나와야 함. 기존 게임과 형식이 많이 다르면 화면을 새로 만들어야 해서 시간이 더 필요
2. **Caddy basic auth 해제 여부** (7번 섹션 참고)
3. **보석금 미달성분 처리** — 못 채운 금액은 어떻게? (신랑 술값 / 그냥 소멸 / 벌칙)
